proc factorial(n: int) do
    let x = 1;

    for i in 2..=n do
        x = x * i;
    end

    action print: @str(x);
end

proc main() do
    for nth in 0..10 do
        factorial(nth);
    end
end

run main;
