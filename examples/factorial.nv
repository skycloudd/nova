proc factorial(n: int) do
    let x = 1;

    for i in 2..=n do
        x = x * i;
    end

    action print: x;
end

proc main() do
    for nth in 0..10 do
        action print: nth;

        factorial(nth);
    end
end
