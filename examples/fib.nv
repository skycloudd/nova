proc fib(n: int) do
    let a = 0;
    let b = 1;

    for n in 0..n do
        let c = a + b;
        a = b;
        b = c;
    end

    action print: a;
end

proc main() do
    for n in 0..20 do
        fib(n);
    end
end

run main();
