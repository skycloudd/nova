const one = 1;
const six = 2 * 3;
const number = one + six;

print number == 7;

for x in one..number do
    print x;
end

action wait: 1.0;

action waitframes: 30;

action move: player, {0.0, 10.0}, false, 0.1;
